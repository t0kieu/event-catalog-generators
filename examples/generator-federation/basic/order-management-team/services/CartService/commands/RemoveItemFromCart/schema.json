{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["cartId", "itemId"],
  "properties": {
    "cartId": {
      "type": "string",
      "description": "Unique identifier of the cart"
    },
    "itemId": {
      "type": "string",
      "description": "Unique identifier of the item to remove from cart"
    }
  },
  "additionalProperties": false
}
