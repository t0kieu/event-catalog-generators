{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["cartId", "productId", "quantity", "timestamp"],
  "properties": {
    "cartId": {
      "type": "string",
      "description": "Unique identifier for the cart"
    },
    "productId": {
      "type": "string",
      "description": "Unique identifier for the product added to cart"
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of items added to cart"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the item was added to cart"
    }
  }
}
